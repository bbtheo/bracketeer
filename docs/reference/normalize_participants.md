# Normalize participants input

Normalize participants input

## Usage

``` r
normalize_participants(participants)
```

## Arguments

- participants:

  Character vector or data.frame with a 'name' column. Optional 'seed'
  and 'rating' columns are preserved.

## Value

List with `names` and `data` (data.frame).
