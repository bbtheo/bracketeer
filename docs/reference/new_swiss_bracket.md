# Internal swiss bracket constructor

Internal swiss bracket constructor

## Usage

``` r
new_swiss_bracket(
  participants,
  rounds = NULL,
  seed = TRUE,
  allow_ties = TRUE,
  bye_points = 1,
  best_of = NULL,
  tiebreakers = NULL
)
```

## Value

A swiss_bracket object.
