# Internal single-elimination bracket constructor

Internal single-elimination bracket constructor

## Usage

``` r
new_single_elim_bracket(
  participants,
  seed = TRUE,
  third_place = FALSE,
  best_of = NULL,
  reseed = FALSE
)
```

## Value

A single_elim_bracket object.
