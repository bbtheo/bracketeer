# Default tiebreakers per format

Default tiebreakers per format

## Usage

``` r
default_tiebreakers(format)
```

## Arguments

- format:

  Tournament format

## Value

Character vector of tiebreakers
